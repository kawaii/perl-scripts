use strict;
use warnings;
use diagnostics;

use feature 'say';
use feature 'switch';

print "Hello World\n";

my $name = 'Kane';

my ($age, $street) = (22, '12 Fake Street');

my $my_info = "$name lives at $street\n";

print $my_info;
